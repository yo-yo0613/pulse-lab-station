# PULSE LAB Audio Directory
Place custom audio files here (e.g., boom.mp3, shutter.mp3, countdown.mp3) if `soundMode` is set to `'custom'` in `src/config/theme.config.ts`.
By default, the system uses the 100% Zero-Dependency Web Audio API synthesizer.
